<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="180dp"
    android:layout_margin="12dp"
    app:cardCornerRadius="16dp"
    app:cardBackgroundColor="#E7D7FD"
    app:cardElevation="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="180dp"
        android:padding="16dp">

        <!-- Время -->
        <LinearLayout
            android:id="@+id/timeLayout"
            android:orientation="vertical"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <TextView
                android:id="@+id/eventStartTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="10:00"
                android:textColor="#000000"
                android:textSize="18sp"
                android:layout_marginBottom="8dp"/>

            <TextView
                android:id="@+id/eventEndTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="16:00"
                android:textColor="#000000"
                android:textSize="18sp"/>
        </LinearLayout>

        <!-- Вертикальная линия -->
        <View
            android:id="@+id/separator"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="#F22242"
            app:layout_constraintStart_toEndOf="@id/timeLayout"
            app:layout_constraintTop_toTopOf="@id/timeLayout"
            app:layout_constraintBottom_toBottomOf="@id/timeLayout"
            android:layout_marginStart="12dp"/>

        <!-- Контент -->
        <LinearLayout
            android:id="@+id/textLayout"
            android:orientation="vertical"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:gravity="center_vertical"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/separator"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:id="@+id/eventTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="«Технологии будущего: Отечественные инновации»"
                android:textColor="#000000"
                android:textStyle="bold"
                android:textSize="20sp"
                android:layout_marginBottom="8dp"/>

            <TextView
                android:id="@+id/eventType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Выставка"
                android:textColor="#F22242"
                android:textSize="18sp"
                android:layout_marginBottom="6dp"/>

            <!-- Контейнер: место + иконка -->
            <LinearLayout
                android:id="@+id/placeAndIconLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/eventPlace"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Выставочное пространство, 2 этаж"
                    android:textColor="#555555"
                    android:textSize="14sp"
                    android:layout_marginEnd="8dp"/>

                <ImageView
                    android:id="@+id/favoriteIcon"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="center_vertical"
                    android:contentDescription=""
                    android:src="@drawable/ic_favorite" />
            </LinearLayout>

        </LinearLayout>


    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
